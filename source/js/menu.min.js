/*var navMain = document.querySelector('.main-nav');
var navMain = document.querySelector('.main-nav');
var navToggle = document.querySelector('.main-nav__toggle');

navMain.classList.remove('main-nav--nojs');

navToggle.addEventListener('click', function() {
  if (navMain.classList.contains('main-nav--closed')) {
    navMain.classList.remove('main-nav--closed');
    navMain.classList.add('main-nav--opened');
  } else {
    navMain.classList.add('main-nav--closed');
    navMain.classList.remove('main-nav--opened');
  }
});*/

var siteList = document.querySelector('.site-list');
var userList = document.querySelector('.user-list');
var navToggle = document.querySelector('.main-nav__toggle');

siteList.classList.remove('main-nav--nojs');
userList.classList.remove('main-nav--nojs');

navToggle.addEventListener('click', function() {
  if (siteMain.classList.contains('main-nav--closed')) {
    siteMain.classList.remove('main-nav--closed');
    siteMain.classList.add('main-nav--opened');
  } else {
    siteMain.classList.add('main-nav--closed');
    siteMain.classList.remove('main-nav--opened');
  }
  if (userMain.classList.contains('main-nav--closed')) {
    userMain.classList.remove('main-nav--closed');
    userMain.classList.add('main-nav--opened');
  } else {
    userMain.classList.add('main-nav--closed');
    userMain.classList.remove('main-nav--opened');
  }
});
